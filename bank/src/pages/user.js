import Header from '../Header/Header';
import Account from '../Account/Account';

const User_page = () => {
    return(
        <section>
        <Header />
        <Account />
        </section>
    )
}

export default User_page;